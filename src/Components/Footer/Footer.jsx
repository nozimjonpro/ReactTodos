// import React from "react";
// import "../Todos/todos.scss";
// import Button from "../Button/Button";

// function Footer({todo}) {
  
//   return (
//     <div className="buttons" style={{display: todo.length > 0 ? 'block' : 'none'}}>
//         <span className="buttons__span"> {todo.length} items left</span>
//       <Button todos = {todo}>All</Button>
//       <Button todos = {todo}>Active</Button>
//       <Button todos = {todo}>Completed</Button>
//     </div>
//   );
// }

// export default Footer;